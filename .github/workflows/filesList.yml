name: filesList

on:
  push:
  workflow_dispatch:
  schedule:
    - cron:  '0/5 * * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: lllll
        run: ls
      - name: Every time
        run: echo "This step will always run"
